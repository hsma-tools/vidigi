# Archived R environment

R/renv support for comparing vidigi against R packages (bupaR, processanimateR) was
removed from the active docs build after repeated CI build failures. These files are
kept for reference in case R support is revived. See CONTRIBUTING.md's "Reviving R
support" section for how to restore them.
